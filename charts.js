export function drawLine(c,p,l){const x=c.getContext('2d'),W=c.width,H=c.height;x.clearRect(0,0,W,H);x.fillStyle='#0b1324';x.fillRect(0,0,W,H);if(p.length<2){x.fillStyle='#9eabc7';x.font='24px system-ui';x.fillText('Faltan datos',24,50);return}const v=p.map(q=>q.v),mn=Math.min(...v),mx=Math.max(...v),pd=Math.max(1,(mx-mn)*.15),lo=mn-pd,hi=mx+pd;x.strokeStyle='#263653';for(let i=1;i<5;i++){let y=H*i/5;x.beginPath();x.moveTo(40,y);x.lineTo(W-15,y);x.stroke()}x.strokeStyle='#63e6be';x.lineWidth=4;x.beginPath();p.forEach((q,i)=>{const a=45+i*(W-70)/(p.length-1),b=H-30-(q.v-lo)*(H-55)/(hi-lo);i?x.lineTo(a,b):x.moveTo(a,b)});x.stroke();x.fillStyle='#eef3ff';x.font='18px system-ui';x.fillText(l(p.at(-1)),46,28)}export const e1rm=(w,r)=>w*(1+r/30);
