export function drawLine(canvas,points,label){const ctx=canvas.getContext('2d'),W=canvas.width,H=canvas.height;ctx.clearRect(0,0,W,H);ctx.fillStyle='#0d1426';ctx.fillRect(0,0,W,H);if(points.length<2){ctx.fillStyle='#9eabc7';ctx.font='24px system-ui';ctx.fillText('Faltan datos',24,50);return}const vals=points.map(p=>p.v),min=Math.min(...vals),max=Math.max(...vals),pad=Math.max(1,(max-min)*.15),lo=min-pad,hi=max+pad;ctx.strokeStyle='#2a3655';ctx.lineWidth=1;for(let i=1;i<5;i++){let y=H*i/5;ctx.beginPath();ctx.moveTo(40,y);ctx.lineTo(W-15,y);ctx.stroke()}ctx.strokeStyle='#63e6be';ctx.lineWidth=4;ctx.beginPath();points.forEach((p,i)=>{const x=45+i*(W-70)/(points.length-1),y=H-30-(p.v-lo)*(H-55)/(hi-lo);i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.stroke();ctx.fillStyle='#eef3ff';ctx.font='18px system-ui';ctx.fillText(label(points.at(-1)),46,28)}
export const e1rm=(load,reps)=>load*(1+reps/30);
